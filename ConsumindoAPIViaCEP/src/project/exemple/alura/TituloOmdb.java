package project.exemple.alura;

public record TituloOmdb (String title, String year, String runtime){

    /* Record trata-se de uma classe imultável, com diversos métodos padrões já embutidos.
        Utilizado, por exemplo, para simplesmente traduzir ou transferir dados e informações de
        um lugar para outro.
        Pode ser conhecido como DTO (data transference object).
     */
}
